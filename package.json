{
  "name": "haetae-quiz-pet-mvp",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "app",
    "app-appintoss",
    "functions",
    "shared"
  ],
  "scripts": {
    "dev": "npm run dev --workspace app",
    "dev:appintoss": "npm run dev --workspace app-appintoss",
    "build": "npm run build --workspace shared && npm run build --workspace app && npm run build --workspace functions",
    "build:appintoss": "npm run build --workspace app-appintoss",
    "typecheck": "npm run typecheck --workspace shared && npm run typecheck --workspace app && npm run typecheck --workspace functions"
  }
}
